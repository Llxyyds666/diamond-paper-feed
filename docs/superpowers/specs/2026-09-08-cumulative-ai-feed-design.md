# 累计中文订阅与复用评测结果

## 用户已确认的目标

把评测 34175877785 中通过新版筛选的 15 篇同步至固定的 ai_summary_feed.xml，并将正式发布改为累计保留。复用已有结果，本次不请求模型，不新增 API 费用。

## 发布约定

- 用户于 2026-09-08 书面确认修改：旧 10 篇先不并入。正式订阅从新评测的 15 篇开始累计；旧记录、旧摘要和既有判定保留在 state.json 和 Git 历史，不删除、不伪造为不相关。
- config/ai_publication.json 记录暂缓发布条目的稳定身份标识；直到用户要求解除暂缓才重新并入。暂缓仅影响发布，不改学术相关性判断。
- 使用 state.json 中已接受的中文摘要重建累计 RSS 与 HTML，不以单次最新结果覆盖历史。
- 沿用 DOI、arXiv 和 Figshare 标识去重；同一论文不重复发布。后续重新判为不相关的条目不再进入当前订阅，但 Git 历史保留旧版。
- AI RSS 与 HTML 累计保留全部已入选条目；原始候选 RSS 的 2000 条上限保持不变。
- 概览只描述本次新增；页面明确标注累计总量，避免让读者以为全部内容都是今天新增。

## 评测转正式

- 验证评测完成、记录身份、结论字段及重复别名一致性，再导入现有结果。
- 同步本评测全部 40 条原始记录的既有判定：16 条相关映射到 15 篇唯一论文，其余 24 条不相关。已处理项从正式待处理队列移除，避免以后再次付费处理。
- 不改写历史 ai_usage.json；评测调用与费用继续由独立评测报告记录，不能冒充此次导入的新费用。
- 不修改原始论文内容、其他待处理论文、采集游标或既有评测结果。
- 状态、RSS、HTML 原子发布；导入重复执行不增加记录，失败不破坏上一版结果。

## 方案选择

采用“状态中的审核结果作为唯一来源、每次重建累计输出”。单纯拼接 XML 难以同步重新判定和状态；另设一份独立历史数据库会引入双份状态，本次不采用。

## 验收

跨两轮处理后两轮入选均存在；当天无新入选不清空历史；同一 DOI/arXiv 别名仅发布一次；第二轮 AI 请求不包含历史入选；离线导入只发布新 15 篇、保留旧 10 篇数据、清理对应待处理项但不改变账本；非法或失败评测拒绝发布；正式 URL 返回 200 且 RSS/HTML 都有 15 篇。验证中不调用真实 AI。
